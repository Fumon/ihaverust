# Maintainer: Jade Bilkey <herself@thefumon.com>
pkgname=ihaverust
pkgver=0.0.1
pkgrel=1
pkgdesc="A dummy package for when you're managing your own rust and cargo install and want to build local packages"
arch=('any')
license=('unlicence')
provides=('rust' 'cargo' 'rust-nightly' 'cargo-nightly' 'rustfmt')
conflicts=('rust' 'cargo' 'rustfmt' 'rustup')

package() {
	/bin/true
}
